## Packages
recharts | Dashboard charts (Line, Bar, Pie)
framer-motion | Page transitions and animations

## Notes
- "Embed Code" for widgets will use an iframe pointing to `/embed/:widgetId`
- Authentication uses session cookies (credentials: "include")
- Dashboard needs aggressive caching or manual invalidation on lead updates
